## Downloading datasets

We assume a `DATA_PATH` environment variable pointing to the data root:

```bash
export DATA_PATH=/home/you/semg_data
```
Datasets are stored under ${DATA_PATH}/{dataset_name}/{raw,processed}

