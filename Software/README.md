This folder contains the KiCAD schematics and the Arduino code for testing the operation of the ADS1298 with an ESP32-S3-DevKit C

The test code is twofolds : 
1. Test register read/write (harmless) wihtout getting into the ADC inner workings yet
2. Digitalize internally generated test signal from the ADS1298 to validate data processing pipeline

   
